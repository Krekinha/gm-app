{
  "name": "init-database",
  "version": "1.0.0",
  "description": "Script para inicializar o banco de dados PostgreSQL",
  "scripts": {
    "init-db": "bash scripts/init-database.sh",
    "db:migrate": "npx prisma migrate dev",
    "db:generate": "npx prisma generate",
    "db:seed": "curl -X POST http://localhost:3001/api/contratos/inicializar",
    "db:studio": "npx prisma studio"
  }
}
