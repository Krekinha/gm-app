# Database Configuration
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/gm_app?schema=public"

# Database Environment Variables (for Docker Compose)
DB_NAME=gm_app
DB_USER=postgres
DB_PASSWORD=postgres

# Redis Configuration
REDIS_URL="redis://localhost:6379"
